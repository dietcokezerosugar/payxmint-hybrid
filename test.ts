const { PaymentEngine } = require('./src/services/payment-engine/PaymentEngine');
// Since it's typescript, we can't just require it. I'll use ts-node or just run it via next.

// Wait, Next.js can run a one-off script?
// Or I can add a temporary API endpoint to test it and hit it with fetch!
